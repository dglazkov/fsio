{
  "name": "@fsio/workbench",
  "version": "0.0.1",
  "private": true,
  "description": "fsio measurement workbench (the client library lives in @fsio/client)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "wireit",
    "preview": "vite preview"
  },
  "wireit": {
    "build": {
      "command": "tsc --pretty && vite build --logLevel warn",
      "files": ["src/**", "index.html", "vite.config.ts", "tsconfig.json", "../../tsconfig.base.json"],
      "output": ["dist/**"],
      "clean": "if-file-deleted",
      "dependencies": ["../client:build"]
    }
  },
  "dependencies": {
    "@fsio/client": "*",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  }
}

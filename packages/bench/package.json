{
  "name": "@fsio/bench",
  "version": "0.0.1",
  "private": true,
  "description": "fsio benchmarks and protocol integration smoke test",
  "type": "module",
  "scripts": {
    "build": "wireit",
    "test": "wireit"
  },
  "wireit": {
    "build": {
      "command": "tsc --pretty",
      "files": ["src/**", "tsconfig.json", "../../tsconfig.base.json"],
      "output": ["dist/**"],
      "clean": "if-file-deleted",
      "dependencies": ["../common:build"]
    },
    "test": {
      "command": "node dist/test-smoke.js",
      "files": ["../host/src/**", "../common/src/**"],
      "output": [],
      "dependencies": ["build", "../host:build"]
    }
  },
  "dependencies": {
    "@fsio/common": "*"
  }
}
